# 后端 V0.3.34+ sprint 候选清单（2026-07-29）

> 📍 路径：`docs/V0.3.34-sprint-candidates.md`
> 上下文：qm-admin 11/11 GAP 全 closed（V0.3.34），后端可启动新 sprint
> 状态：待主人决策方向

---

## 🎯 当前后端状态

- **HEAD commit**：`22126a6` V0.3.11 后端（生产 healthy） + 后续 `d6e5ec8` V0.3.29 UI 修复
- **最新生产部署**：V0.3.29 healthy（init #22 后）
- **funcs%**：87.74% 实测（init #17 lcov）/ 1427 passed + 62 skipped = 1489 total
- **module 数**：36 + 1 拆分（ai-coach / stats / device / ai-相关）
- **GAP**：全部 closed（X 1 个 init #22 误报 GAP-11 已纠正）

---

## 📋 Sprint 候选（按业务价值排序）

### 候选 A：admin MIS 增强（续 V0.3.4-5）
**业务价值**：⭐⭐⭐⭐⭐（高 — 运营效率直接提升）
**工作量**：⭐⭐⭐（3-5 commit）
**风险**：低

**可做事项**：
1. **admin.dashboard 实时刷新**：30s 自动 poll + WebSocket 推送（已有 V0.2.116 通道）
2. **admin.users 详情页**：单个用户的训练/订单/积分/分销全维度
3. **admin.orders 批量操作**：多选 + 批量状态扭转 + 批量退款
4. **admin.globalSearch 增强**：支持 type filter + 时间范围 + 跨表高亮
5. **admin.dashboard 图表可视化**：用 antd Charts/Recharts 加折线/饼图
6. **admin.excel 导出**：订单/用户/提现/结算单 CSV/Excel（V0.3.29b CSV 已有）
7. **admin.audit_logs 增强**：按时间/操作人/目标 过滤 + 详情查看
8. **admin.config 远程热更新**：feature_flags 不需重启即可生效

**前置依赖**：无
**预计工期**：2-3 sprint
**GAP 影响**：qm-admin 前端需要加新 page 或扩展现有 page

---

### 候选 B：性能优化（funcs 87.74 → 92%+）
**业务价值**：⭐⭐⭐（中 — 用户体验提升）
**工作量**：⭐⭐⭐（2-3 commit）
**风险**：低

**可做事项**：
1. **Cache 接入更多热路径**：当前 58 处 Cache.wrap，V0.2.3 后 0 新增，可加：
   - `admin.globalSearch`（搜索结果缓存 60s）
   - `admin.dashboard`（9 字段缓存 30s）
   - `stats.dailyReport`（AI 解读缓存 1h）
   - `aiCoach.contextBuilder`（prompt 缓存）
2. **DB 查询优化**：Prisma query 加 select 限制字段 + 索引 review
3. **N+1 查询检测**：`sport.checkin` + `user.profile` + `feed.list` 常用 N+1
4. **分页优化**：大表（Checkin/Order）cursor-based pagination 替代 offset
5. **限流加固**：`@fastify/rate-limit` 接入 admin login + user login（防爆破）
6. **API 响应压缩**：`@fastify/compress` gzip middleware

**前置依赖**：无
**预计工期**：1-2 sprint
**GAP 影响**：无（纯后端优化）

---

### 候选 C：国际化（i18n）
**业务价值**：⭐⭐⭐（中 — 海外用户扩展）
**工作量**：⭐⭐⭐⭐（5-10 commit，全量文案提取）
**风险**：中（架构调整）

**可做事项**：
1. **i18n 框架选型**：`@nestjs/i18n` / `node-i18n` / `i18next` 评估
2. **文案集中管理**：`locales/{zh-CN,en-US,ja-JP}/` 目录
3. **API 错误消息 i18n**：Zod 错误 / Errors 类多语言
4. **DB 字段多语言**：Content.title_en / title_jp（V0.1.122 content 已有）
5. **小程序前端配套**：apps/miniprogram 用 i18next 切换 zh/en
6. **qm-admin 配套**：qm-admin Web 用 react-intl

**前置依赖**：架构调整 + 团队熟悉 i18n 库
**预计工期**：3-5 sprint（大改动）
**GAP 影响**：全栈（后端 + miniprogram + qm-admin）

---

### 候选 D：mobile-adapter（适配 V0.2.67-72 Flutter APP）
**业务价值**：⭐⭐⭐⭐（高 — 沐禾健康 Android 客户端）
**工作量**：⭐⭐⭐⭐（4-6 commit）
**风险**：中

**可做事项**：
1. **后端 API 适配 Flutter Phase 1.5 微信登录**：
   - 现有 `/api/auth/login` 已支持 phone/email/password
   - 加 Wechat App OAuth（移动 App 端 `wx.login` 走服务端 exchange）
2. **后端 response 格式适配 Flutter dio**：
   - 统一 `{code, data, msg}` envelope（已有）
   - 错误码 i18n（Flutter 端多语言）
3. **WebSocket 适配 Flutter**：
   - `infra/realtime.ts` 加 Flutter 平台 token 校验
   - `services/realtime.ts` Flutter 端示例代码
4. **图片上传 CDN 适配**：
   - Flutter 端直传 COS（已有 `infra/cos.ts` STS 签名）
5. **Push Notification 集成**（V0.2.142 移除 WechatSI 后未补）：
   - 极光/JPush/小米推送 选型
   - 后端 `services/notification/` 适配多通道

**前置依赖**：Flutter Phase 1.5 完成
**预计工期**：2-3 sprint
**GAP 影响**：Flutter 客户端

---

### 候选 E：AI 私教深化（V0.1.139 + V0.3.10 续）
**业务价值**：⭐⭐⭐⭐（高 — AI 是核心差异化）
**工作量**：⭐⭐⭐（3-5 commit）
**风险**：中

**可做事项**：
1. **多 provider 抽象**：V0.1.139 GLM + V0.2.33 MiniMax M3 + 未来 Claude
2. **多模态增强**：
   - 图片识别（V0.2.45 GLM-4.6V）
   - 视频识别（运动视频分析）
   - 语音输入（V0.2.44 已去 voice，GAP-18 闭后可重加）
3. **个性化 prompt 优化**：基于用户历史 30 天数据 + 6 维度画像（V0.1.139 已有）
4. **AI 主动建议**：不是被动回复，而是定时推送（"今天适合跑步吗"）
5. **AI 教练切换**：V0.1.140 4 人设（科学家/教练/伙伴/严师），加新角色（心理咨询师/营养师）
6. **训练计划生成升级**：V0.1.140 generatePlan 基础版 → 多目标规划（健身 + 跑步 + 减重）

**前置依赖**：V0.3.10 会员分层 + V0.2.45 多模态
**预计工期**：2-4 sprint
**GAP 影响**：apps/miniprogram + qm-admin

---

### 候选 F：设备生态扩展（V0.1.43 + V0.3.13/20 续）
**业务价值**：⭐⭐⭐⭐（高 — 设备数据是核心）
**工作量**：⭐⭐⭐⭐（4-6 commit）
**风险**：高（设备 SDK 差异）

**可做事项**：
1. **华为运动健康 Cloud API OAuth 2.0 接入**（V0.3.19 骨架 + 实际 API 接入）
2. **Apple HealthKit 接入**（iOS 端用户）
3. **小米穿戴 Cloud API 接入**（V0.1.128 Terra 之外）
4. **COROS Terra OAuth 接入**（V0.1.128 + V0.2.71 hotfix 续）
5. **设备 OTA 升级**（V0.3.20 authCenterList 之外）
6. **多设备数据融合**：心率带 + 手表 + 手机三方数据合并
7. **设备授权中心增强**（V0.3.20）：OAuth 跳转 + 设备解绑

**前置依赖**：主人物料（华为/小米/COROS API 申请）
**预计工期**：3-5 sprint
**GAP 影响**：apps/server device + admin + apps/miniprogram device-auth

---

### 候选 G：技术债清理
**业务价值**：⭐⭐（低-中 — 长期质量）
**工作量**：⭐⭐⭐（3-5 commit）
**风险**：低

**可做事项**：
1. **TypeScript 严格模式**：`tsconfig.json` strict: true 全开
2. **ESLint 全栈**（apps/server 已有 .eslintrc 5 行，apps/miniprogram V0.3.29 已补）
3. **依赖升级**：Prisma 5.x → 6.x / Fastify 4.x → 5.x（V0.3.x 仍是 4.x）/ Node 18 → 22 LTS
4. **TypeScript path alias 标准化**：`@qm-wx/shared` 统一跨仓库
5. **CI 强化**：coverage report 上传 Codecov + parallel test split
6. **测试覆盖率真实提升**：qm-admin 88% → 92-94%
7. **DEAD CODE 清理**：unused exports + unused imports
8. **BullMQ 监控**：worker lag + failed jobs 告警

**前置依赖**：无
**预计工期**：2-3 sprint
**GAP 影响**：全栈

---

## 🎯 推荐组合（按价值/工作量）

**如果选 1 个**：**候选 A**（admin MIS 增强）— 业务价值最高，qm-admin 前端可立即受益

**如果选 2 个**：**A + E**（MIS + AI 私教）— 主线业务 + AI 差异化

**如果选 3 个**：**A + B + E**（MIS + 性能 + AI）— 平衡业务 + 性能 + 创新

---

## 📋 决策点（待主人拍板）

请主人选择 1-N 个 sprint 方向（可叠加），或提出新方向：
- 候选 A：admin MIS 增强
- 候选 B：性能优化
- 候选 C：国际化
- 候选 D：mobile-adapter
- 候选 E：AI 私教深化
- 候选 F：设备生态扩展
- 候选 G：技术债清理
- 新方向（请说明）

---

🤙 决策后我会按 /zcf:workflow 启动新工作流（研究 → 构思 → 计划 → 执行 → 优化 → 评审）。