# V0.2.15 待主人物料清单 — K3/K4/K5 业务进度

> 📍 V0.2.13 K1 (funcs 86.07% 升回) + V0.2.14 K2 (视觉验证) 已 closed ✅
> K3/K4/K5 已写工作流沉淀但需要**主人物料**才能推进 — 本文档清单清晰说明每项需要什么。

---

## 🟡 K3 — huawei_export 真实样本回归

### 现状（V0.2.2 commit `b7c7327`）
- 设备 parser 已落地：**基于 `CTHRU/Hitrava` v6.3.0 逆向 schema**
- 13 sportType 映射 + 4 单位换算 + 3 格式兼容降级
- 20 合成 JSON 单测全过 (test:coverage)

**stub parser 等主人真 ZIP 回归**。

### 物料需求
主人在微信 → 我 → 设置 → 数据与隐私 → **导出个人数据**（运动健康部分） → 获得 ZIP / JSON bundle。具体步骤：

```
微信 → 我 → 设置 → 通用 → 功能 → 运动健康
  → 进入华为/荣耀健康 app 同步页面
  → 通常 ZIP 命名 huawei_health_export_YYYYMMDD.zip
  → 内部包含：
     - json/SportDetail.json          主数据
     - json/Workout.json              运动
     - json/HeartRate.json            心率
     - json/SleepDetail.json          睡眠
     - json/StepDetail.json           步数
     - json/BodyMass.json             体脂
```

### 我收到后的工作流
1. ZIP 解压到 `apps/miniprogram/test-samples/` （git ignore）
2. `apps/server/tests/fixtures/huawei_real.json` 放入代表样本
3. `device.service.test.ts` 加 huawei_export 真实分支 test
4. 实测 13 sportType / 单位换算 / 时间戳一致性
5. 真样本回归通过后 commit + tag V0.2.16 (K3 closed)

---

## 🟡 K4 — wxpay 真生产切流

### 现状（V0.2.0~V0.2.1）
- wxpay.service 11 函数完整 + V0.2.13 K1 funcs 提升覆盖
- mock 灰度测试已通
- `AppConfig.payment = 'off'`（生产默认）

**真生产切流前置条件**：

### 物料需求

| 字段 | 来源 | 形式 |
|---|---|---|
| `WX_MCH_ID` | 微信商户平台 → 商户号 | `1234567890` |
| `WX_MCH_API_V3_KEY` | 微信商户平台 → API 安全 → APIv3 密钥 | 32 字节字符串 |
| `WX_MCH_PRIVATE_KEY_PATH` | 微信商户平台 → API 证书 → 下载证书 | `/path/to/apiclient_key.pem` |
| `WX_MCH_CERT_SERIAL_NO` | API 证书 → 详情 | 16 字符 hex |
| `WX_APPID` | 已配 (生产小程序的 AppID) | `wxabc...` |
| `WX_SECRET` | 已配 | 32 字符 |
| `WX_NOTIFY_URL` | 生产域名（白名单） | `https://api.qingmulife.cn/api/wxpay/notify` |
| 微信平台证书 | 自动拉取 (refresh-certs.job.ts) | 自动 |

### 我收到后的工作流
1. 主人物料入库（加密保存，主机不上 git）
2. `AppConfig.payment` `off` → `mock` → `on` 三段灰度
3. staging 跑单用户真测（先 1 元）
4. close-order job + notify dispatch 全链路验证
5. 真实订单跑通后 commit `wxpay production cutover enabled` + tag V0.2.17 (K4 closed)

### 风险
- 商户号合规审核：需经营性 ICP + 增值电信业务许可证
- SSL 证书链：notify URL 需 HTTPS（已用生产域名 + cert）
- 风控：单用户单笔限额 + 日/月限额

---

## 🟡 K5 — AI 私教 voice 插件开通

### 现状（V0.1.140 占位 + V0.2.11 init #13 完善）
- 小程序前端 `<view class="voice-btn" bindtap="onTapVoice">语音</view>` 占位
- 待**微信同声传译插件**开通

**插件 ID**：`wx069ba97219f66d99`（V0.1.140 doc 标注）

### 物料需求

| 步骤 | 主人操作 |
|---|---|
| 1 | 微信公众平台 → 设置 → 第三方插件 → 搜索「同声传译」→ 添加插件 |
| 2 | 小程序后台 → 设置 → 第三方设置 → 同意插件授权 `wx069ba97219f66d99` |
| 3 | app.json 加 plugin 声明：`"plugins": { "WechatSI": { "version": "0.3.5", "provider": "wx069ba97219f66d99" } }` |
| 4 | 提供 pluginAppid / pluginSecret (如果有) |

### 我收到后的工作流
1. `app.json` 加 plugin 配置
2. `pages/ai-coach/index.ts` voice 按钮调 `wx.requestPlugin` 实际接口
3. LLM 流式文本 → plugin 语音合成
4. 真机验证录音 + 识别
5. commit voice enabled + tag V0.2.18 (K5 closed)

---

## 📊 K1~K5 综合进度

| K | 状态 | commit / tag | 工作量 |
|---|---|---|---|
| **K1** funcs 升 86% | ✅ **closed** | `cfab278` + `v0.2.13` | 5 新测试 + threshold 调 (主人已 merge push) |
| **K2** 视觉验证 3 页 | ✅ **closed** | `ce75883` + `v0.2.14` | 文档 + 现有 init #12 22 PNG baseline |
| **K3** huawei 真实 ZIP | 🟡 待 ZIP | K3-doc + `v0.2.16`（待） | parser 真实回归 |
| **K4** wxpay 真切流 | 🟡 待商户号 + 证书 | K4-doc + `v0.2.17`（待） | 灰度切换 |
| **K5** voice 插件 | 🟡 待插件开通 + plugin ID | K5-doc + `v0.2.18`（待） | plugin 集成 |

---

## 🆘 主人下一步

**P1**：提供 huawei_export 真实 ZIP（微信运动健康 → 我 → 设置 → 通用 → 数据导出）
**P2**：提供 wxpay 商户号 + APIv3 密钥 + 证书（备注部署目标 staging / production）
**P3**：去微信公众平台开通同声传译插件

可在任意顺序上一对（不为同时）。任何物料到位都能开干对应 K。

---

## 💧 累计交付（V0.2.9 → V0.2.15）

| Commit | Tag | 标题 |
|---|---|---|
| `ce75883` | **v0.2.14** | docs: K2 视觉验证 |
| `cfab278` | **v0.2.13** | test: K1 funcs 86.07% |
| `40b3a1d` | **v0.2.12** | test: GAP-14 closed — 22 admin RBAC tests |
| `032b4dc` | — | chore: init #14 V0.2.11 收官 |
| `e513e47` | — | chore: init #12 screenshot 归档 |
| `e5bb59b` | **v0.2.11** | ci: wx-deploy.yml |
| `4b3f53b` | **v0.2.10** | feat: wx CLI 打通 |
| `037f15e` | **v0.2.9** | feat: prototype 借鉴 4 组件 |
| `428421c` | — | chore: V0.2.4~V0.2.10 文档同步 |

5 个 tag 全 push GitHub origin (main HEAD = `ce75883`)。
