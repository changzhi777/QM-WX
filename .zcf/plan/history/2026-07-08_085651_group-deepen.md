# 跑群深化 MVP（V0.1.42，方案 1）

> sport group 基础已完整（createGroup/joinGroup/quitGroup/groupRanking + Group/GroupMember 表），补全 3 缺口：群详情卡 + 成员列表 + 群公告

## 范围
- Group +announce 字段（1 迁移，不增表）
- sport +3 action：groupDetail（群卡+汇总）/ groupMembers（成员列表含本月跑量）/ announceGroup（owner 发公告）
- 前端 group-detail 改造（群卡 + 公告 + 汇总 + 成员列表，不增页）
- 测试 +4

## 步骤
1. Prisma schema Group +announce + 迁移 `20260707170000_group_announce`
2. sport.schema +3 schema
3. sport.service +3 action（groupDetail/groupMembers/announceGroup）
4. sport.routes +3 case
5. shared endpoints sport +3 action
6. 前端 group-detail 改造
7. 测试 +4
8. 验证（prisma generate/migrate + test + typecheck）

## 关键决策
1. Group +announce 字段（不增表，KISS）
2. groupMembers 复用 familyRanking `groupBy userId` 范式（DRY，N+1 规避）
3. announceGroup 鉴权 `role === 'owner'`（复用 sportRepo.isMember 返 role）

## 预期数字
- 表 45 不变（Group +字段）/ sport action 5→8 / 页 38 不变 / 测试 572→~576 / 迁移 18→19
