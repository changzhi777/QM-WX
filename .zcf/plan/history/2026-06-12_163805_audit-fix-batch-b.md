# 计划：QM-WX 全栈整顿 · 方案 B

> 📅 创建时间：2026-06-12 15:55:45
> 🎯 任务名：audit-fix-batch-b
> 📋 方案选择：方案 B（全栈整顿 3-4 个工作日）

## 上下文

### 任务背景

用户调用 `/zcf:workflow` 全面审查 QM-WX 主仓 + qm-admin 独立仓两个项目，要求：
- 审查覆盖：两仓全审（QM-WX monorepo + qm-admin）
- 交付形式：全套 6 阶段走到落地（代码层面）
- 测试优化重点：覆盖率补齐 + E2E 场景补齐 + 测试代码重构 + CI 启动 e2e

### 研究结论（来自 4 个并行 Explore agent）

| 仓库 | 评级 | 测试覆盖 | P0 | P1 | P2 |
| --- | --- | --- | --- | --- | --- |
| QM-WX `apps/server` | B+ | 86.28% / 201 单元 + 5 e2e | 3 | 4 | 5 |
| QM-WX `apps/miniprogram` | B | 0 | 1 | 4 | 4 |
| qm-admin | B- | 0 | 4 | 4 | 8 |

### 方案 B 范围

P0 8 项全清 + P1 9 项 + 测试基础设施重构 + CI 拆 parallel job。

---

## 执行步骤（10 个原子任务）

### Task #44 测试基建（最先做，其他依赖）

**目标**：抽出 helpers + fixtures，让后续补测试用例 3 倍效率。

**原子步骤**：
1. 建 `apps/server/tests/helpers/mockErrors.ts` — 抽 15+ 文件重复的 `Errors` mock
   - 暴露 `mockErrors` 对象（notFound / forbidden / badRequest / unauthorized / payRequired / featureDisabled）
2. 建 `apps/server/tests/helpers/mockPrisma.ts` — Prisma + transaction mock 工厂
   - 暴露 `createPrismaMock(modelNames)`，返回 `{ prismaMock, txMock, $transactionMock }`
3. 建 `apps/server/tests/fixtures/` 目录
   - `user.fixture.ts` — `makeUser(overrides?)` / `makeUserOutput()`
   - `order.fixture.ts` — `makeOrder()` / `makeOrderItem()`
   - `product.fixture.ts` — `makeProduct()` / `makeCategory()`
   - `group.fixture.ts` — `makeGroup()` / `makeGroupMember()` / `makeCheckin()`
4. **逐步**把 wallet.service.test.ts 改用新 helpers（作为示范）
   - 不大规模重构所有测试（保留可用即可），新写的测试一律走 helpers/fixtures
5. Redis mock 统一为 `vi.mock('src/infra/redis.js')` 风格
   - 删除 `vi.mock('ioredis')` 写法（仅 redis.test.ts 保留，因为测的就是构造）

**预期产出**：
- `tests/helpers/` 2 文件
- `tests/fixtures/` 4 文件
- 旧测试不动（避免回归），新测试沿用

**预期结果**：30 测试 + 5 e2e 全绿、coverage 不掉。

---

### Task #37 P0-1：小程序 ENDPOINTS 4 缺口

**目标**：消硬编码 URL + 补缺失 action 常量。

**原子步骤**：
1. 编辑 `packages/shared/src/api-contracts/endpoints.ts`
   - 加 `auth: { refresh: '/api/auth/refresh' }`
   - 加 `user.me`、`sport.myGroups`、`sport.today`（统一指向 `/api/user`、`/api/sport`）
2. 重新 build shared：`pnpm --filter @qm-wx/shared build`
3. 编辑 `apps/miniprogram/miniprogram/services/api.ts`
   - 把 `wx.request({ url: 'http://.../api/auth/refresh' ... })` 改为 `ENDPOINTS.auth.refresh`
4. 跑小程序 typecheck：`cd apps/miniprogram && npx tsc --noEmit`

**预期结果**：typecheck 全绿，所有 wx.request 都走 ENDPOINTS。

---

### Task #38 P0-2：qm-admin Login 安全 + 死依赖 + access

**目标**：消 6 个 P0（Login 安全 / user.id='unknown' / zustand / access 空挂）。

**原子步骤**：
1. 编辑 `qm-admin/src/pages/Login.tsx`
   - 用户提交后，先 `POST /api/user {action: 'me'}` 拿真实 user（id + nickname + avatarUrl）
   - 失败（401/admin 不在 whitelist）则提示并清 token
   - 成功才写 localStorage + setInitialState
2. 编辑 `qm-admin/src/services/user.ts`（新建）— 暴露 `getMe()` wrapper
3. 编辑 `qm-admin/src/access.ts`
   - 接 `/api/admin {action: 'listAdmins'}` 查 openid 是否在 whitelist
   - 但调用要在 getInitialState 里做一次缓存，避免每次 access 都打接口
4. 编辑 `qm-admin/src/app.tsx`
   - getInitialState 增加 `isAdmin: boolean` 字段，调 listAdmins 验证 openid
   - access.ts 用 `initialState.isAdmin`
5. 编辑 `qm-admin/package.json`
   - 删 `zustand` 依赖
6. `npm install` 重建 lockfile
7. typecheck：`npx tsc --noEmit`

**预期结果**：登录走真实校验，access 真正生效，无 zustand。

---

### Task #41 P1-A：后端公开端点 + isAdmin 缓存 + recipe Zod

**目标**：消后端 4 个 P1。

**原子步骤**：
1. 重构 `apps/server/src/modules/content/content.routes.ts`
   - 把 `list`/`detail` 拆为独立 GET 路由（带 `config: { public: true }`）
   - 或：在 action handler 内部，对 list/detail 路径前 `req.routeOptions.config.public = true`
   - 推荐：拆独立路由更清晰
2. 同样重构 `apps/server/src/modules/mall/mall.routes.ts`
   - listCategories / listProducts / productDetail 拆 GET 公开路由
3. `apps/server/src/modules/admin/admin.routes.ts` 加 isAdmin 缓存
   - 参考 `common/middleware/feature-gate.ts` 的 `_cache` + `invalidateXxxCache` 模式
   - 在 `setConfig` action 写 `admin_whitelist` 时清缓存
4. `apps/server/src/modules/recipe/recipe.routes.ts` myMeals
   - 补 Zod schema：`since: z.string().regex(/^\d{4}-\d{2}-\d{2}$/).optional()` 同 until
5. 跑后端 typecheck + test：`pnpm --filter @qm-wx/server typecheck && pnpm --filter @qm-wx/server test`

**预期结果**：游客可以访问 content/mall 列表 API，typecheck + 测试全绿。

---

### Task #42 P1-B：小程序三态 + feature-gate + agreement + 去冗余

**目标**：消小程序 4 个 P1。

**原子步骤**：
1. 11 页 error 状态 UI
   - 抽通用 `<error-state>` 组件（components/error-state/）— 显示 ❌ + 提示文字 + 重试按钮
   - 各页 catch 增加 `setData({ error: true, errorMsg: e.message })`
   - WXML 加 `<error-state wx:if="{{error}}" message="{{errorMsg}}" bind:retry="loadData" />`
2. `pages/product-detail` + `pages/order-confirm` 改用 `<feature-gate>` 组件
   - product-detail 购买区域包 `<feature-gate flag="payment">...</feature-gate>`
   - order-confirm 提交按钮包同样
   - 删除 TS 层 `canBuy`/`canPay` 状态（仅 UI 层判断）
3. `pages/agreement` 补内容
   - 加 WXML：用户协议 + 隐私政策标题 + 段落（mock 文本 8-10 段）
   - 或：调 `POST /api/content {action: 'list', payload: {type: 'agreement'}}` 拉取
4. `pages/mine` 去冗余
   - 删 TS 层 `goWallet/goMembership/goBindApp` 的 flag 判断（已被 feature-gate 隐藏）
5. typecheck：`cd apps/miniprogram && npx tsc --noEmit`

**预期结果**：error 状态全覆盖，feature-gate 一致接入，无冗余判断。

---

### Task #43 P1-C：qm-admin location/并发/null/nginx

**目标**：消 qm-admin 4 个 P1。

**原子步骤**：
1. `qm-admin/src/app.tsx` request 拦截器
   - 401 不再 `location.href`，改用 `history.push('/login')`
2. `qm-admin/src/pages/mall/Orders.tsx`
   - 状态扭转加 `useState<Record<string, boolean>>` 记录 orderId 的"扭转中"状态
   - Select 在 loading 时 `disabled`，避免并发
3. `qm-admin/src/pages/mall/Products.tsx`
   - `originalPrice` 处理：`editTarget.originalPrice != null ? Number(...) : undefined`
   - 同样处理 `memberDiscount`
4. `qm-admin/deploy/nginx.conf` envsubst
   - 把 `host.docker.internal:3000` 改为 `${BACKEND_URL}`
   - Dockerfile 加 `CMD ["/bin/sh", "-c", "envsubst '${BACKEND_URL}' < /etc/nginx/conf.d/qm-admin.conf.tpl > /etc/nginx/conf.d/qm-admin.conf && nginx -g 'daemon off;'"]`
   - 或更简：用 nginx 1.27 自带的 entrypoint envsubst 机制（COPY 到 `/etc/nginx/templates/qm-admin.conf.template`）
5. typecheck：`npx tsc --noEmit`
6. docker build 验证

**预期结果**：所有 P1 落地，docker build 仍通过。

---

### Task #39 P0-3：后端测试 3 个缺口（依赖 #44）

**目标**：补 wallet routes + mall routes + auth code2Session 失败路径。

**原子步骤**：
1. `apps/server/tests/modules/wallet/wallet.routes.test.ts` 补
   - `consumeInTx` 路由层（虽然是内部方法，但 wallet 模块如有 HTTP 入口要测）
   - 实际 wallet.routes 没暴露 consumeInTx HTTP，重点是 `refund` 在 V1 下也是 feature-gated（写一行测试覆盖即可）
2. `apps/server/tests/modules/mall/mall.routes.test.ts` 新建
   - listCategories / listProducts / productDetail 路由层
   - createOrder / myOrders / cancelOrder 也补
   - 用 fixtures/product + fixtures/order
3. `apps/server/tests/common/integrations/wx/code2session.test.ts` 增强
   - 已有 3 个测试：正常 / errcode / 无 openid
   - 补：fetch 抛错（网络失败）/ JSON 解析失败 / 超时
4. 跑覆盖率：`pnpm --filter @qm-wx/server test:coverage`
   - 期望 `mall.routes.ts` 从 2.38% → ≥80%

**预期结果**：3 文件新增/扩充，全绿，覆盖率上升。

---

### Task #40 P0-4：Happy Path E2E（依赖 #44）

**目标**：补完整 mall-flow 链路 E2E。

**原子步骤**：
1. 新建 `apps/server/tests/e2e/mall-flow.e2e.test.ts`
2. 场景：
   ```
   ① 登录（mock code2Session → 拿 token）
   ② GET listProducts → 拿到商品列表
   ③ POST mall create order（积分全额抵扣）→ 拿 orderId
   ④ GET myOrders → 验证订单存在、状态 pending_pay
   ⑤ POST mall cancel → 取消
   ⑥ 验证：积分回退（数据库查 user.points 恢复 + PointsRecord 有 refund 记录）
   ```
3. 数据准备：
   - beforeAll: 建 1 user（带初始积分 500） + 1 product
   - afterAll: 清理 user/order/product/pointsRecord
4. 跑：`RUN_E2E=1 pnpm --filter @qm-wx/server test mall-flow`

**预期结果**：完整链路打通，全绿。

---

### Task #45 CI 拆 parallel job（依赖 #39 + #40）

**目标**：unit/e2e 拆分提速反馈。

**原子步骤**：
1. 编辑 `.github/workflows/ci.yml`
   - 把 `lint-typecheck-test` 拆为：
     - `lint-typecheck`：lint + typecheck（最快，~2 分钟）
     - `unit-tests`：单元测试（不需要 PG/Redis services）
     - `e2e-tests`：e2e（需要 PG/Redis services）
   - `unit-tests` 和 `e2e-tests` 都 `needs: lint-typecheck`，但彼此 parallel
   - `build-server` 改 `needs: [lint-typecheck, unit-tests, e2e-tests]`
2. 跑一下 lint：`cd apps/server && pnpm lint`（确认 CI 现在跑的命令格式）
3. 提交 + 观察 CI（如有问题回滚）

**预期结果**：CI 总时长不增加但反馈快（unit 5 分钟内、e2e 单独跑）。

---

### Task #46 优化 + 评审 + 归档

**目标**：[模式：优化] + [模式：评审] + 计划归档。

**原子步骤**：
1. 优化阶段：扫一遍本次产生的代码
   - 查重复 mock / 重复 type / 重复 helper
   - 查 `as any` 是否能消
2. 评审阶段：对照本计划逐项核对
   - 列出未完成项 + 原因
   - 列出额外做的 + 是否值得保留
3. 更新 `MEMORY.md` 索引 + 写新 memory：`audit-batch-b-complete.md`
4. 更新根 CLAUDE.md changelog
5. 把本文件从 `.zcf/plan/current/audit-fix-batch-b.md` 移到 `.zcf/plan/history/[完成时间]_audit-fix-batch-b.md`

---

## 依赖关系图

```
Task #44 (测试基建) ──┬─→ Task #39 (后端测试补)
                     └─→ Task #40 (Happy E2E)
                                  ├──→ Task #45 (CI parallel)
Task #37 (小程序 ENDPOINTS) ───────┤
Task #38 (qm-admin 安全) ──────────┤
Task #41 (后端 P1) ───────────────┤
Task #42 (小程序 P1) ─────────────┤
Task #43 (qm-admin P1) ───────────┴──→ Task #46 (优化 + 评审 + 归档)
```

并行机会：
- 第 1 批：Task #44（单独跑，最关键）
- 第 2 批：Task #37 / #38 / #41 / #42 / #43 完全并行（不同仓库不同文件）
- 第 3 批：Task #39 / #40 并行（同后端不同文件，需先有 #44）
- 第 4 批：Task #45（CI 配置）
- 第 5 批：Task #46（收尾）

## 风险与对策

| 风险 | 对策 |
| --- | --- |
| 测试 mock 重构破坏现有测试 | 增量改：先建 helpers，再示范改 1 个文件（wallet.service），其他不动 |
| qm-admin 加 listAdmins 校验导致循环（access 调 API 但 API 需要 access） | getInitialState 阶段做一次性 fetch，存 isAdmin 到 initialState |
| nginx envsubst 改动可能 break docker | 改完立即 docker build + run 验证 |
| Happy E2E 数据清理失败导致下次 run 冲突 | 数据 prefix 全用 `e2e-mall-` + afterAll 强删 |
| CI parallel 后构镜像 needs 改错 | 改完先在 PR 上跑一次再 merge main |

## 不在范围内（明确排除）

- 18 个 P2（组件抽取、`as any` 全消、columns 拆文件等）
- qm-admin / 小程序的单元测试覆盖（方案 C 的内容）
- Sentry / OpenTelemetry 监控接入
- packages/shared 拆 admin 类型 + qm-admin npm link
- Phase 4 支付实质接入（仍按原节奏）

## 完成判定

✅ 所有 10 个任务 completed
✅ 后端 30 单元 + 6 e2e 全绿（+1 mall-flow）
✅ 覆盖率 ≥ 86%（不掉）
✅ 小程序 typecheck 全绿
✅ qm-admin typecheck 全绿 + docker build 通过
✅ CI 跑通（lint-typecheck → unit + e2e parallel → build）
✅ 本文件归档到 `.zcf/plan/history/`
