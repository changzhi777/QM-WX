# 完善设备绑定 — BLE 品牌识别（佳明手表 + 小米手环）

> 📅 启动：2026-07-03 16:19:58 CST
> 🎯 版本：V0.1.33
> 📋 模式：/zcf:workflow 方案 1（前端为主，后端轻改 MVP）

## 上下文

用户需求：完善设备绑定功能，基于小程序蓝牙扫描绑定佳明手表和小米手环。

### 4 决策
1. **vendor 设计**：方案 C（vendor=品牌 key `garmin`/`xiaomi`/`ble`，与佳明现有 OAuth 数据 RawActivity 无冲突）
2. **数据范围**：方案 B（心率 0x180D + 电量 0x180F + 设备信息 0x180A）
3. **佳明 BLE/OAuth 展示**：方案 A（BLE 绑定优先，OAuth 数据降级显示）
4. **品牌识别**：方案 C（设备名自动识别 + 未识别手选兜底 + 0x180A Manufacturer Name 二次验证）

## 执行计划（10 步）

### 阶段 A：共享层
- **A1**. `packages/shared/src/constants/device-brands.ts`：xiaomi available=true；加 `BLE_VENDOR_PATTERNS` + `matchBleVendor(name)` 函数

### 阶段 B：后端
- **B1**. `device.schema.ts`：`BindBleDeviceInputSchema` 加 vendor 参数 + brandMeta optional
- **B2**. `device.service.ts`：`bindBleDevice` 接受 vendor 按 [userId,vendor] upsert；`myBindings` garmin BLE 优先（garminBleBound + garminOAuthCount）
- **B3**. `device.service.test.ts`：补 2 用例（bindBleDevice vendor + myBindings garmin BLE 优先）

### 阶段 C：前端 BLE 工具
- **C1**. `utils/ble.ts`：加 `readBattery(deviceId)` + `readDeviceInfo(deviceId)`（0x180F/0x180A）

### 阶段 D：前端绑定页
- **D1**. `device-bind/index.ts`：品牌识别 + 手选 actionSheet + 多服务读取 + bindBleDevice 传 vendor
- **D2**. `device-bind/index.wxml`：扫描结果品牌标签 + 电量/型号展示
- **D3**. `device-bind/index.wxss`：品牌标签样式（三色）+ 电量展示

### 阶段 E：验证 + 文档
- **E1**. server tsc + vitest + shared build + mp-shared + miniprogram typecheck
- **E2**. 根 CLAUDE.md changelog V0.1.33 + 子文档（init-architect）+ memory v0133

## 改动汇总
- 文件：~8（shared 1 + 后端 3 + 前端 4）+ 文档
- **schema 改动：0**（零迁移，方案 1 MVP）
- 预期测试增量：+3-5

## 状态
- [x] 研究（5/10 → 4 问题补充）
- [x] 构思（方案 1 vs 2，选 1）
- [x] 计划（10 步批准）
- [ ] 执行（进行中）
- [ ] 优化
- [ ] 评审
