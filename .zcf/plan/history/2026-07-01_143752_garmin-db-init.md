# 佳明数据建库 + 初始化云 PG + 查询 API（方案 B-2）

> 创建：2026-07-01 11:42:48 CST
> 工作流：`/zcf:workflow`（研究→构思→计划→**执行**→优化→评审）
> 数据源：`佳明手表数据包/`（张晨，userProfileId 11032831，2022-2026）

## 上下文
- 佳明账户导出包：Activity(618) + Sleep(13 段/~千日) + FitnessAge(467) + Metrics(8 类) + Gear/PR
- 云 PG：qingmulife.cn（腾讯云 106.53.168.73，PG16 容器内网，**不暴露端口**）
- 关联：云 PG 现有 User「张晨」（seed 自 memory `cloud-api-db-link`）

## 锁定决策
- 表设计：**B-2**（核心厚表 Sleep/FitnessAge + Metrics 通用宽表；Activity 复用 `RawActivity`）
- 数据范围：核心 4 类 + Gear/PR 附赠；Golf/Social/Routing 丢弃
- 用户归属：关联张晨（vendor=garmin, vendorUserId=11032831）
- 执行：本地脚本 → SSH 隧道连云 PG
- 查询 API：**首轮接 Cache.wrap**（Redis）
- 前端：本轮只后端 API + shared 契约 + api.ts 封装，页面另开

## 步骤清单
- **A 建库**：① schema 加 3 表（GarminSleep/GarminFitnessAge/GarminMetric）+ User 反向关系；② migration
- **B 灌数据**：③ ingest 脚本（解析+upsert+Cache 失效）；④ 本地 dry-run(stats)；⑤ 云 PG 执行（SSH 隧道 + pg_dump 备份 + migrate deploy + ingest）；⑥ 验证
- **C 查询 API**：⑦ shared endpoints device.* + Zod；⑧ device.service 4 方法（含 Cache.wrap 300s）；⑨ device.routes 4 case（requireLogin）；⑩ 小程序 api.ts + 单测/e2e

## 缓存设计
- key：`garmin:{activities|sleep|metrics|fitnessAge}:{userId}:{paramsHash}`
- TTL 300s（历史数据，比 sport 60s 长）；ingest 后 `delByPattern('garmin:*:{userId}:*')`；fail-open
- 热路径 10 → 14

## 执行顺序
A(建表) → C(查询API，空表可测) → B(灌数据，云执行)

## 风险
- distance 单位待 dry-run 核对（样本 800356 异常）
- 生产写入 → dry-run + pg_dump + 幂等 upsert
- 回滚 → DELETE WHERE userId / DROP TABLE 3 表 / migrate resolve --rolled-back
